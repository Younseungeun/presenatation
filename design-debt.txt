# 실행으로 검증되지 않은 설계 결정 — **갚아야 할 빚의 명세**
#
# `constants-debt.txt`가 "근거 없는 숫자"를 세는 것과 같은 장치다. 이쪽이 세는 것은
# **한 번도 돌아본 적 없는 결정**이다. 6차 검토(docs/review-request-model-6.md)에서
# 여섯 회차의 결정 대부분이 실행된 적 없는 코드 위에 서 있다는 것이 드러나 만들었다.
#
# ── 검증 단계 (6차 F-2 확정 · 11차 K-5에서 한 칸 추가) ────────────
#   [Type]   타입 체커·린트·단위 시험만 통과. 그 경로가 실제로 돈 적은 없다
#   [Mock]   외부 API·모델 없이 로컬 E2E 실행 완료 (스텁·픽스처 포함)
#   [Staged] **코드는 완결됐고 내가 돌릴 수 없다** — 실행 권한이 나에게 없는 것
#   [Live]   실제 모델·API로 실측값을 얻음
#
# ── [Staged] 를 왜 따로 두는가 (11차 K-5) ─────────────────────────
# 시스템 설정을 바꾸는 산출물(작업 스케줄러 등록, 방화벽, 서비스 계정)은 **내가 실행할
# 수 없다.** 그런데 그걸 [Type]으로 두면 "아직 안 짰다"와 "짰는데 못 돌린다"가 같은
# 칸에 들어가고, 둘은 남은 일이 전혀 다르다 — 앞은 내 몫이고 뒤는 창업자의 몫이다.
#
# **[Staged] → [Live] 로 올리는 조건**: 창업자가 실제 환경에서 실행하고, 그 **출력
# 로그와 상태 확인 결과를 저장소에 붙인다.** "돌렸다"는 말만으로는 안 올린다 —
# 이 스레드가 아홉 회차 동안 배운 것이 정확히 "각 조각이 옳아도 시스템은 틀릴 수
# 있다"이고, 남이 돌린 결과를 못 본 채 [Live]로 적는 것이 그 계열의 실수다.
#
# 지금 [Staged]: **없음**
#
# ~~sidecar/install-task.ps1~~ — **[Live] 갚음 (2026-08-20, 11차)**
#   창업자가 관리자 PowerShell에서 실행했고, 그 출력과 상태를 확인했다:
#     Status: Ready → 실행 중 · Task To Run: watchdog.ps1
#     PID 26128 · 127.0.0.1:8765 LISTENING
#     model_sha a050925fc384da78 · ready true · 카나리아 3건 통과 (최대 오차 4.87e-07)
#     probeStudentWiring: 규칙 0건 / 학생 1건 (확신 70%) → 배선 확인
#
#   ⚠ **다만 절반만 [Live]다.** 등록된 작업의 로그온 모드가 `대화형만`이라,
#     부팅 트리거는 **창업자가 로그인한 뒤에야** 발동한다. 재부팅하고 로그인하지
#     않으면 사이드카는 안 뜬다. 로그인 없이 띄우려면 자격 증명을 저장해야 하고
#     (서비스 계정), 1인 운영 데스크톱에서는 그 대가가 얻는 것보다 크다고 봤다.
#     **실패 시 자동 재시작(1분)은 아직 실측하지 않았다** — 재시작을 유발하려면
#     프로세스를 죽여야 하고 그러면 실제 경보가 창업자 폰으로 나간다.
#
# **"시험이 초록불"과 "돌아간다"는 다르다.** 이 저장소는 1,254건이 초록불인데
# 값어치가 걸린 경로(API 호출·학습·추론)는 한 번도 실행되지 않았다.
# 등급을 적는 목적은 그 사실이 매번 눈에 보이게 하는 것이다.
#
# ── 갚는 순서 (6차 F-2 + 8차 E-1) ─────────────────────────────────
# 위험도(되돌리기 비용)도 실행 가능성도 아니라 **다음 단계의 블로커를 무엇이 푸는가**로
# 정한다. 스키마가 없으면 그림자 기록을 못 남기고, 기록이 없으면 졸업 조건을 못 잰다.
#
# **다만 그 앞에 오는 것이 하나 있다 (8차 E-1).**
# 검토 답변이나 할 일을 줄 세울 때 먼저 묻는다: *이것이 실패하면 무슨 일이 일어나는가?*
#
#   지표가 안 오른다            → **성능**  (데이터 추가, 하이퍼파라미터)      … 나중
#   지금까지의 숫자가 전부 무효  → **유효성·동등성**                          … **먼저**
#
# 두 번째 칸에 드는 것들: 학습셋과 평가셋의 분리 · 학습 환경과 서빙 환경의 계산 동등성
# (PyTorch↔ONNX 로짓 대조) · 토크나이저 지문 · 채점 코드가 재려던 것을 실제로 재는가.
#
# 7차 검토가 로짓 대조를 시켰고 나는 "다음에"로 미뤘다. 그 검사는 5분이었고, 실패했을 때
# 무효가 되는 것이 **그 회차의 모든 측정**이었다. 대가는 한 회차였다.
# **자가 똑바른지 확인한 뒤에 물건을 올려놓는다.**
#
# 확인: 이 파일과 docs/model-plan.md의 결정 표를 함께 본다
#
# ═══════════════════════════════════════════════════════════════
# [Mock] 대기 — 지금 갚을 수 있는 것
# ═══════════════════════════════════════════════════════════════
#
# ~~training/export_onnx.py — ONNX 변환~~ — **[Live] 갚음 (2026-08-19, 8차)**
#   [Mock]로도 세지 않고 있었는데, 재보니 **돌아가는 것처럼 보이면서 틀린 답을 내고
#   있었다**: 내보낸 그래프가 입력을 읽지 않아 서로 다른 문장에 비트 단위로 같은 값을
#   냈다(PyTorch 대조 오차 0.376). 7차와 8차 초반의 하네스 수치 전부가 그 위에서 나왔다.
#   원인은 퇴화한 더미 입력(torch.ones 하나를 두 입력에 그대로).
#   이제 내보내기가 ① 입력 반응성 ② PyTorch 대조(1e-4)를 검사하고 실패하면 배포하지
#   않는다. 실측: 반응 0.573 / 오차 2.4e-06.
#   **교훈은 등급 체계 자체에 관한 것이다** — [Live]는 "실측값을 얻었다"인데, 이 경로는
#   실측값을 얻고 있었고 그 값이 가짜였다. **재는 자를 검사하는 단계가 등급에 없다.**
#
# ~~학생 모델 채택 판정~~ — **[Live] 갚음 (2026-08-19, 8차)**
#   문장 7유형이 홀드아웃(학습에 한 건도 안 들어간 손코퍼스 128건)에서 채택선을 넘었다:
#   합산 탐지 53.8% → 84.6%, 패러프레이즈 0% → 66.7%, **합산 오탐은 17.6% 그대로**
#   (학생 단독 오탐 0.0%). 지연 P50 9~10ms.
#   CARD_MISMATCH는 **미졸업** — 정상 문서 34건을 전부 걸어 배포에서 뺐다
#   (STUDENT_ENABLED_LABELS). 문서 단위 탐지율은 여전히 0%다.
#
# ~~학생 소견을 실제로 켜는 것~~ — **결정·구현 완료 (8차 E-6)**
#   ⓑ 바로 켠다(WARN 전용). STUDENT_MODE 기본 live, 되돌리기는 배포가 아니라 환경 변수.
#   검토안에 없던 관문을 하나 더 달았다 — 지문 대조(usable())가 그림자 서비스 안에만
#   있어서, 그대로 켰다면 토크나이저가 갈라진 날 조용히 틀린 답이 남의 게시를 막았다.
#   시험 19건이 "학생은 거절시키지 못한다 / 죽어도 게시를 막지 않는다"를 못 박는다.
#
# **[Live] 대기 — 운영 데이터 0건**
#   여기서부터는 코드로 갚을 수 없다. 진짜 리서처의 문장이 들어와 운영자 판정이 붙어야
#   비로소 알 수 있는 것들:
#     · 비용비 λ의 실측값 (지금 4는 측정이 아니라 선언이다 — evalStudent COST_RATIO 주석)
#     · 학생 오탐률의 모집단 값 (지금은 정상 34건에서 0건. 다음 34건에서 1건이 나올 자리다)
#     · 문장 코퍼스 밖의 문체·주제 분포
#   그림자 표(ShadowComplianceReview)는 이제 STUDENT_MODE=shadow 일 때만 쓰인다 —
#   라이브에서는 본 기록에 source='student'로 들어가고 운영자 판정이 그 행에 붙는다.
#
# ~~prisma/migrations/…_shadow_compliance_review~~ — **[Mock] 갚음 (2026-08-19)**
#   마이그레이션 적용 완료(76개 정상), 조회 검증 완료. 그림자 기록 경로도 함께 붙었다:
#   src/server/shadowScreeningService.ts + src/infra/compliance/studentClient.ts,
#   시험 8건이 "어떤 실패도 게시를 막지 않는다"를 못 박는다
#
# ~~sidecar/app.py — 토크나이저 스텁 사이드카~~ — **[Mock] 갚음 (2026-08-19)**
#   파일 반입 완료 + venv 구성 완료(transformers 5.15.0, fastapi 0.141.1) + 실제 기동.
#   **이 프로젝트에서 처음으로 추론 경로가 끝까지 돌았다**: Node 어댑터 → localhost HTTP
#   → 토크나이저 → 응답. 지문 a053f457838bdbe0, 지연 평균 0.20ms(목표 5ms 이하).
#   남은 것은 가중치뿐이다 — 학습이 끝나면 model.int8.onnx만 놓으면 stub이 false가 된다.
#
# training/train.py · export_onnx.py — 문법 검사만 통과
#   막힌 이유: torch·transformers 미설치, `python`은 Windows 스텁, `pip`가 PATH에 없다
#   갚는 법: `py -m venv .venv` → `.venv\Scripts\pip install -r requirements.txt`
#            (사설 PyPI 또는 .whl 반입) + local_models/ 반입
#
# ═══════════════════════════════════════════════════════════════
# [보류] 미사용 경로 — 갚지 않는다 (2026-08-19 사용자 확정)
# ═══════════════════════════════════════════════════════════════
#
# **운영에 Anthropic API를 쓰지 않기로 확정했다.** 구독으로 학습 데이터를 만들고,
# 출시 전까지 학생 모델을 완성해 2차 검수를 그것이 맡는다. 그러면 아래 항목들은
# "안 쓰는 전화선의 통화료를 아끼는 코드"가 된다 — 지우지는 않는다(출시 후 API를
# 쓰기로 하면 그대로 살아난다). 다만 **빚이 아니므로 갚는 순서에서 뺀다.**
#
# src/infra/compliance/claudeScreener.ts — 프롬프트 캐싱 (cache_control)
#   손익분기 히트율 21.7%를 넘는지가 미검증이고, **그 베팅을 건 코드 경로도 미검증**이다.
#   위험이 두 겹. 마이크로 배치 10건이면 hit_tokens가 찍히는지까지 확인된다
#
# src/infra/compliance/claudeScreener.ts — EFFORT (medium)
#   건당 비용의 43%를 정하는 값인데 한 번도 재지 않았다
#
# src/infra/compliance/screener.ts — screeningCostUsd()
#   계산식이 API 영수증과 일치하는지 확인된 적 없다. 마이크로 배치의 첫 검증 대상
#
# ═══════════════════════════════════════════════════════════════
# [Mock] 대기 — 구독만으로 갚을 수 있는 것
# ═══════════════════════════════════════════════════════════════
#
# 교사 기준선 (문장 86 · 문서 42) — **깨끗한 세션 라벨로 갚는다**
#   2차 H-2의 "대화 라벨은 기준선 불가"는 과했다. 막을 것은 대화가 아니라 오염이었고,
#   저장소를 본 적 없는 새 창은 코퍼스를 읽을 방법이 아예 없다.
#   갚는 법: 새 창에 pack-N.md 붙여넣기 → label:ingest -- --clean-session
#   ※ 채택선이 "≤ 교사"에서 "≤ 규칙 보류율 + 패러프레이즈 > 0%"로 바뀌어
#     이 값은 이제 **문턱이 아니라 참고선**이다 (없어도 학생 채택은 가능하다)
#
# ═══════════════════════════════════════════════════════════════
# [Type] — 실행 경로가 아예 열린 적 없는 것
# ═══════════════════════════════════════════════════════════════
#
# scripts/trainSynthesize.ts — 합성 생성·라벨링 (API 키 필요)
# src/domain/studentText.ts:buildStudentText — 학습·추론이 실제로 같은 문자열을 만드는지 미확인
#   (사이드카가 서면 [Mock]으로 갚힌다 — 같은 함수의 출력을 양쪽에서 대조)

# ═══════════════════════════════════════════════════════════════
# 10차에 정해진 두 규칙 (2026-08-20)
# ═══════════════════════════════════════════════════════════════
#
# ── ① 방어 장치를 만들 때 묻는 질문 (I-4) ──────────────────────
#
#   "이 방어의 **식별자(이름)를 유지한 채**, 검사 대상의 **논리만 바꿔치기**할 수 있는가?"
#
# 방어가 **외부의 이름**(파일 이름, 포트 번호, 경로)에 기대면 언제나 열린 상태라
# 검사를 하나씩 더 다는 무한 목록이 된다. 방어가 **대상의 본질적 속성**(가중치의 sha,
# 정답 로짓, 토크나이저 내용 해시)과 묶이면 그 안에서 닫힌다.
#
# 세 회차 연속으로 나온 결함이 전부 같은 모양이었다 — "이름은 맞는데 내용이 다르다":
#   8차 학습셋=채점지 / 8차 죽은 그래프 / 9차 옛 가중치 / 10차 **카나리아 자신**
# 마지막 것이 결정적이다: 그 계열을 잡으려고 만든 장치가 `model_file: "model.onnx"` 라는
# **이름**으로 자기를 식별하고 있었다. 방어를 만들면 그 방어에 같은 질문을 한 번 더 한다.
#
# 재는 법: 이름을 유지한 채 내용을 훼손하고 **돌연변이 생존율**을 잰다(기대 0%).
#   → training/mutation_test.py — 실측 0/20. 다만 층을 나눠 재라:
#     로짓 대조만으로는 9/10이 살아남았다(파라미터 1,400만에 카나리아 입력 3건).
#
# ── ② 형식 검사와 내용 평가의 경계 (I-3) ───────────────────────
#
#   **내부적 모순 = 형식.  외부적 진실 = 내용.**
#
# 리서처가 쓴 것이 시장에 비추어 맞는지 우리가 판단하면 → **내용 평가(투자자문)**.
# 한 문서 안에서 두 값이 어긋난 것을 지적하면 → **페이로드 무결성(형식)**.
#
# CARD_MISMATCH 결론부 규칙이 어휘를 "방향을 암시하는 말"이 아니라 **투자 판단을 명시하는
# 말**로 좁혀 둔 것이 정확히 이 선이다. **양쪽 어휘가 함께 있으면 지적하지 않는 것**도
# 같은 이유 — 어느 쪽이 최종 결론인지 가리는 것은 내용 판단이다.
#
# 이 선을 넘는 규칙을 새로 넣지 마십시오. 넘었는지는 이렇게 묻는다:
#   *이 규칙이 틀렸다고 말하려면 시장을 봐야 하는가, 문서만 보면 되는가?*
