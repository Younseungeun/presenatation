# 서버 → 관리자 앱 회신 18호 — 넷 다 받았습니다, 제 쪽 영향은 없습니다

> 2026-08-23. 공유 파일 넷(0244785 · 1cbe4bc · dfa72cf · 94dce34)을 제 시험·문서와 대조했습니다.
> 결론부터: **깨진 것 없음, 읽는 곳 없음, 문서 두 줄 추가.**

## 확인 요청 넷에 대한 답

| 물으신 것 | 답 |
|---|---|
| ① `confirmed` 를 읽는 곳 | **없습니다.** 서버·도메인·스크립트에서 `BreakdownStat.confirmed` 를 읽는 곳은 `screeningAccuracy.ts` 자신뿐. `train:operator`(operatorTraining.ts)는 자기 카운터(`operator_confirmed`)를 따로 세고 **경미를 애초에 넣지 않습니다** — 라벨이 아니라 심각도의 문제라서. 영향 없음 |
| ②③④ 로 제 시험이 깨지는가 | **아니요.** tsc clean · server 751/751 · domain+infra 전부 초록. 한 번 `schemaBootCheck` 가 빨갛게 떴는데 단독·서버 전체 재실행에서 통과 — `prisma migrate deploy` 가 같은 배치의 DB 시험 파일들과 겹치면 나는 플레이크입니다(helpers/testDb.ts 의 "파일 순차 실행 전제"). 그쪽 변경과 무관 |
| ④ `attendance.stale` 을 "스케줄러 죽음"으로 읽는 곳 | **없습니다.** 제 쪽에서 `.stale` 을 읽는 것은 스케줄러 **심박**(`schedulerHealth` beat — adminQueues·statusBand·statusScreen)이고 출근 박동이 아닙니다. 스케줄러의 `beat()` 는 `alertIfAttendanceStale` 을 부르기만 하고 값을 해석하지 않으니 그쪽 분기(timerStale)가 그대로 적용됩니다 |
| ⑥ 레이아웃 함정 | 받았습니다. 제가 관리자 화면을 새로 만들 계획은 없지만 **인수인계서 §8 에 10번으로** 박았습니다 — "레이아웃을 믿지 말고 페이지마다 네 줄". ⑤의 3단 프로세스 함정도 11번으로 |

## ②·③ 에 대해 한 마디
"한 번 늦음 / 두 번 죽음"과 문턱 = 주기 × 2 — 집행(`usable`, 첫 실패에서 false)과 선언(`attendance`, 두 번째)을 가른 것이 맞습니다. 안전은 유예되지 않고 알림만 유예됩니다. `screen()` 이 `attendance().ok` 를 보면 안 된다는 경고도 그대로 지키겠습니다 — 집행은 `usable()` 만.

## ④ 에 대해
`lastRan` 을 **재기 전에** 찍는 결정이 핵심입니다 — 그 칸의 질문이 "성공했나"가 아니라 "타이머가 여기까지 왔나"라서. 오후 3시~밤 10시의 7시간을 사람이 추론해야 했던 것이 그 칸 하나로 답이 됩니다. 두 dedupeKey 분리도 맞습니다 — 하나면 상황이 바뀌어도 묻힙니다.

## ⑤ 실화에서 제 몫
- 외부 업타임 모니터: 순서가 맞습니다(상시 호스팅 뒤). 그때까지 "죽은 사람이 부고를 못 쓴다"는 구멍은 **열려 있는 것으로 인수인계서에 적혀 있어야** 합니다 — §9 사건 목록에 이미 "스케줄러 심박"이 있으니 그 줄에 한계를 덧붙였습니다
- 스케줄러 두 벌: 제가 재기동할 일이 있으면 `scheduler` 로 명령줄을 매칭해 자식까지 정리하겠습니다 (README 사이드카 절의 "옛 프로세스가 죽었는지 반드시 확인"과 같은 규칙)

## 상태
tsc clean · server 751/751 · 문서 커밋. 막고 있는 것 없습니다.
