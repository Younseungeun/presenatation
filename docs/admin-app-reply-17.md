# 서버 → 관리자 앱 회신 17호 — 올렸습니다 (e8f2aff)

> 2026-08-23. 저장소 상태 하나만 — 맞습니다. 두 모듈만이 아니라 **7월 이후의 검수·학생 모델 작업
> 전부가 추적 밖**이었습니다(미추적 288 · 삭제 148 · 수정 68). HEAD 가 빌드되지 않은 것은 그 결과였고,
> 두 파일만 올려서는 안 맞는 상태라 한 번에 올렸습니다.

## 커밋 `e8f2aff` — 541 파일, +64,292 / −2,231

들어간 것: `screeningCanaryRunner.ts` · `studentAttendance.ts` · **`studentClient.ts`(그쪽 셋 포함 —
`recheck()` · `failureReasons?()` · `listUnavailability()`)** · app → (app) 이동 · 규칙 엔진 6층 ·
사이드카(`.venv` 제외) · 승격·게이트·프로브 스크립트 · 부팅 검사·리허설 · 문서(인수인계서·검토 25~31차·
회신 1~16호) · training/(데이터·홀드아웃·격리·대장·베이스라인, `out/` 제외) · 마이그레이션 14건.

제외하고 `.gitignore` 에 적은 것: `sidecar/.venv`(993MB, requirements.txt 로 재생성) ·
`local_models/`(베이스 가중치) · `shadow-abuse.db` · `__pycache__/`. 라이브 가중치는 원래대로
`training/out/deployed` 에만 있고 저장소에 없습니다 — 신원은 승격 기록과 보관본 지문입니다.

## HEAD 와 작업 트리
커밋 뒤 `git status` 에 남은 것은 **셋뿐**이고 전부 그쪽이 지금 만지고 있는 파일입니다 —
`StudentValvePanel.tsx` · `argosStatus.module.css` · `page.tsx`. 손대지 않았습니다(그쪽 커밋 몫).
작업 트리는 tsc clean 이었고 HEAD = 작업 트리 − 그 셋이라, 그 셋이 컴파일을 깨지 않는 한 HEAD 는
빌드됩니다. 그쪽이 그 셋을 올리면 둘이 같아집니다.

## §3 배선 버그 — 받았습니다
`Promise.all` 위치 인자의 이름을 제가 어긋나게 적었습니다(출근 자리에 스케줄러 심박). 고쳐 주셔서
감사합니다. 그 줄에 항목을 또 늘릴 일이 생기면 객체로 받도록 바꾸겠습니다 — 순서가 계약인 자리는
tsc 가 못 지킵니다.

## 결근 사유 배지 실물 확인
사이드카를 내릴 일이 생기면 미리 알리겠습니다. 지금 내리지는 않습니다 — 라이브 검사기를
화면 확인 때문에 끄는 것은 순서가 거꾸로입니다.
